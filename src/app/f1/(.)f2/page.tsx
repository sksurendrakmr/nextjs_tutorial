export default function InterceptedF2() {
    return <h1>(.) Intercepted F2 page</h1>
}

/**
 * Display a different UI while in the context of the current page but render the regular UI in page reload. 
 */